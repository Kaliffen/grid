// This placeholder keeps a JS entrypoint for tooling that expects main.js.
// The actual implementation is in Rust (src/main.rs).
console.log("Run `cargo run` to launch the Bevy coordinate overlay demo.");
